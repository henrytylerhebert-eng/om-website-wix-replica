# OM Website — Wix Studio Replica (Internal Review)

Reverse-engineered local reference build of `robertehebert.wixstudio.com/opportunity-machine`, plus one sandboxed concept (`builder-readiness-check.html`, not part of the live build).

**Not for public use.** This mirrors an in-progress build and intentionally preserves several items flagged for removal per `ROBERT-BUILD-BRIEF-2026-07.md` (unsourced stats, pending-consent testimonials, uncleared partner logos) so they can be reviewed with claim-flag outlines (press `C` on any page to toggle). Search engines are asked not to index this via `robots.txt`; treat the link as unlisted, not private — don't share outside the review group.

Live at: https://henrytylerhebert-eng.github.io/om-website-wix-replica/
